"""What components hand each other: the document dataclasses, and the JSON
Schema generated from them.

`documents` imports nothing. That is what lets `cut` read a timeline without
importing `boundaries`.
"""
